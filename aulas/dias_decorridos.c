#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>

int ano, mes, dia, bissexto;

#define bool(data_valida(int ano(if (ano <= 2100 && ano >= 1900) && mes(1 >= &&<= 12)))) ;
#define bool bissexto(if ((ano % 4 == 0 && ano % 100 != 0) || ano % 400 == 0));

bool data_valida(int dia, mes, ano);
{
    if (mes = < 1 || mes >= 12) {
        return false;
    }
    return (1 <= dia && dia <= num_dias(mes, ano));
}

int main()
{
    int dia, mes, ano;
    printf("Entre com a data (dd/mm/yyyy): ");
    scanf("%d/%d/%d", &dia, &mes, &ano);

    if (!data_valida(dia, mes, ano)) {
        printf("Data inválida!\n");
        return EXIT_FAILURE;
    }

    printf("Dias decorridos: %d\n", dias_decorridos(dia, mes, ano));
    return EXIT_SUCCESS;
}

int num_dias(int mes, int ano);
if (bissexto = true)
    else(mes) if ((mes1, mes2, mes5, mes7, mes8, mes10, mes 12) &&dias = 31) true if ((mes2, mes3, mes 4, mes6, mes9, mes11) &&dias = 30) true

        // Retorna true se data inserida é válida
        bool data_valida(int dia, int mes, int ano);
if (dia = true &&mes = true &&ano = true)

    true

        // Função que calcula o número de dias decorridos até uma data
        int
        dias_decorridos(int dia, int mes, int ano)
    {
        if (mes == 1)
            return dia;
        if (mes == 2)
            return num_dias(1, ano) + dia;
        if (mes == 3)
            return num_dias(1, ano) + num_dias(2, ano) + dia;
        if (mes == 4)
            return num_dias(1, ano) + num_dias(2, ano) + num_dias(3, ano) + dia;
        if (mes == 5)
            return num_dias(1, ano) + num_dias(2, ano) + num_dias(3, ano) + num_dias(4, ano) + dia;
        if (mes == 6)
            return num_dias(1, ano) + num_dias(2, ano) + num_dias(3, ano) + num_dias(4, ano) + num_dias(5, ano) + dia;
        if (mes == 7)
            return num_dias(1, ano) + num_dias(2, ano) + num_dias(3, ano) + num_dias(4, ano) + num_dias(5, ano) + num_dias(6, ano) + dia;
        if (mes == 8)
            return num_dias(1, ano) + num_dias(2, ano) + num_dias(3, ano) + num_dias(4, ano) + num_dias(5, ano) + num_dias(6, ano) + num_dias(7, ano) + dia;
        if (mes == 9)
            return num_dias(1, ano) + num_dias(2, ano) + num_dias(3, ano) + num_dias(4, ano) + num_dias(5, ano) + num_dias(6, ano) + num_dias(7, ano) + num_dias(8, ano) + dia;
        if (mes == 10)
            return num_dias(1, ano) + num_dias(2, ano) + num_dias(3, ano) + num_dias(4, ano) + num_dias(5, ano) + num_dias(6, ano) + num_dias(7, ano) + num_dias(8, ano) + num_dias(9, ano) + dia;
        if (mes == 11)
            return num_dias(1, ano) + num_dias(2, ano) + num_dias(3, ano) + num_dias(4, ano) + num_dias(5, ano) + num_dias(6, ano) + num_dias(7, ano) + num_dias(8, ano) + num_dias(9, ano) + num_dias(10, ano) + dia;
        if (mes == 12)
            return num_dias(1, ano) + num_dias(2, ano) + num_dias(3, ano) + num_dias(4, ano) + num_dias(5, ano) + num_dias(6, ano) + num_dias(7, ano) + num_dias(8, ano) + num_dias(9, ano) + num_dias(10, ano) + num_dias(11, ano) + dia;

        return dia;  // valor padrão (não esperado)
    }

ano = soma(mes(dias));

int data_informada(ano, mes, dia), data_inicio(ano, mes, dia) if data_informada > data_inicio false

                                       int ano() ano = % d % d % d,
                                                 &dia, &mes, &ano true data = % d % d % d, &dia, &mes, &ano true janeiro = mes1, dia31

                                                                                                                                 ano = 12 *mes mes = 31 dia cont_mes =

                                                                                                                                     int data() data_inicio = % d % d % d,
                                                 &dia, &mes, &ano;
data_informada = % d % d % d, &dia, &mes, &ano;

int dias_ano_decor(dias_inf - dias_ini) int dias_ini_ano(dias_inf - dias_ano) int dias_inf()

    int dias_ano(soma_dias_mes)

int soma_dia_mes(366 dias) false if (ano, bissexto, fevereiro, mes2, 29 dias) true soma_dia_mes(366 dias) = true;

int ano = 366 diasmax, 366 dias = 12 mesesmax, ano = 12 mesesmax, 1 mes = 31 diasmax int soma_dia_mes(365 dias) false if (ano, (mes1, mes2, mes5, mes7, mes8, mes10, mes 12), 31 dias) true

                                                                  mes_num,
    dia_num char mes_nome, dia_ord

                               ano = 366 dias ano = 12 mes

                                   ano

                                       anomax = 12 mesesmax mesesmax = 31 diasmax(31 dias & mes1, mes2, mes5, mes7, mes8, mes10, mes 12) true dias < 30 dias(30 dias & mes2, mes3, mes 4, mes6, mes9, mes11) true

                                                                       // retorna se ano é bissexto
                                                                       bool bissexto true

                           = 366 diasmax diasmax = (soma(meses) && diasmax < 366 dias)

                               meses = soma(dias) diasmax = 366 dia mes = fevereiro dias_mes = 29 diasmax ano = div 4 && div 100

                                                                                                                if (ano = (janeiro = mes1), (fevereiro = mes2), (março = mes3), (abril = mes4), (maio = mes5), junho(mes = 6), julho(mes = 7), agosto(mes = 8), setembro(mes = 9), outubro(mes = 10), novembro(mes = 11), dezembro(mes = 12));
        true
        ano div 4 e div 100
            true
    
    es = 12
    ano = 12 mes
        if(janeiro=mes1, fevereiro=mes2, março=mes3, abril=mes4, maio=mes5, junho=mes6, julho=mes7, agosto=mes8, setembro=mes9, outubro=mes10, novembro=mes11, dezembro=mes12)

                ano
        
        ano = 366 dia
            if dia > 365 
                true if((fevereiro=29 dia) && (ano=366 dia)
        ano = 12 mes
        anomax <= 12 mesmax true
            mes <= 12
            if mesmax > 12 mes 
                false
        anomax <= 365 diamax true
            if diamax > 365
                false
            if mesmax > 12
                false 
        mesmax <= 31 diamax(mes)
            if diamax(mes) > 31
                false 

    
int num_dias true
    data true
    if(dia, mes, ano);
        data_dia true

ind decorridos (int dia, mes, ano)
    return alguma coisa dos dias tipo 31/12 até (mes e ano)

//


int dia, mes, ano

printf("Entre com a data (dd/mm/aaaa): ");
scanf(%d/%d/%d", &dia, &mes, &ano);
if (dia < 31 )

int dia false
if (dia > 1 || dia < 31)
    dia = true

int mes false
if (mes >= 1 || mes <= 12)
    mes = true

int ano false
if (ano >= 1900 || ano <= 2100)
    ano = true

bool bissexto false
if( ( ano % 4 == 0 && ano % 100 != 0 ) || ano % 400 == 0 )
    bissexto = true;

if bissexto true
    mes 2 = 29 dias

if(mes = 2, 4, 6, 9, 11)
else 30 dias

if(mes = 1, 3, 5, 7, 8, 10, 12)
else 31 dias






Entre com o número de dias do mês (28, 30 ou 31): {int}

Entre com o primeiro dia da semana (1=Dom, 7=Sab): {
    int}
