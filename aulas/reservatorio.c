#include <stdio.h>

int main()
{
    int n, dias, litros;
    printf("Entre com a quantidade de chuva no dia em litros: \n");

    for (int n = litros; dias < 7; n++) {
        scanf("%d", &litros);
        printf("Entre com a quantidade de chuva no dia em litros: \n");
        if (litros == 400) {
            break;
            printf("O reservatório transbordou no dia: ", % d);
        } else {
            printf("O reservatório não transbordou. Espaço livre: \n", % litros);
        }
    }

    return 0;
}